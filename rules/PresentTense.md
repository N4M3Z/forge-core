Write documentation, code, config, inline comments, commit messages, and ADRs in the present: state what is true now, as if no earlier version existed. In config-file comments, describe only what the line does now.

Write: "Account 39501 has 854K journal entries in Helios production."
Write: "`appearance` is ignored when `theme` is set explicitly above."

If a correction would be cleaner as a rewrite than a patch, rewrite from the current truth. A wrong mental model annotated with corrections is worse than a clean restatement. For what to leave out, see [[NoBackstory]].
