Before claiming a file exists, a function is missing, or a test is absent, verify with the Read or Grep tool. Opus 4.5 occasionally confabulates file contents from training data rather than reading the actual repository state.

When reviewing merge requests, read every changed file completely before producing findings.
