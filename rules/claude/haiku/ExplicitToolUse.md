Always use the Read tool before making claims about file contents. Never infer code structure from file names or directory layout alone.

When asked to review a merge request, read every changed file completely before producing findings. Partial reads lead to false positives.
