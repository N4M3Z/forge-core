Never write real names, email addresses, phone numbers, or credentials into tracked files. Use pseudonyms or placeholders in examples, test fixtures, and documentation.

Exceptions: `plugin.json` author fields and LICENSE headers may use real attribution by design. Secrets belong in `.env` (gitignored), never in source.
