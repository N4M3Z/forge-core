When writing examples, schemas, or sample output in tracked files, never copy real data from the current session context. Replace names, phone numbers, emails, and identifiers with pseudonyms (Alice/Bob/Carol Example, +420600000001, alice@example.com) before writing.

This applies even when the data is "just an example" — tracked files are public, session context is not.
