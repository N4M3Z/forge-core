name: Release

on:
    push:
        tags: ["v*"]

permissions:
    contents: write

jobs:
    release:
        uses: N4M3Z/forge-cli/.github/workflows/module-release.yaml@v0.3.2
        permissions:
            contents: write
